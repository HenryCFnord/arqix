# Notes

This directory is for informal but durable working notes.

Expected content:

- design sketches
- naming decisions that are not yet stable enough for `project/`
- observations from using the tool on its own corpus
- small findings that should not be lost in chat history or commit messages

If a note becomes stable policy, it should move into `project/`.
